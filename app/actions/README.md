Frontend-only migration uses shared utilities in `app/lib/api.ts` for form handling and local persistence.
Server Actions were intentionally not used because the requirement allows utility-based frontend-only logic.
